# KV Store 改进报告

## 结论摘要

本次把原有“能演示 quorum 概念”的实现，完善为行为边界更明确、并发更安全、可故障注入和可重复测量的单 Leader 内存 KV 原型。最关键的修复是：同一 key 的版本分配改为原子操作、复制/读取改为并行 fan-out、旧版本不能覆盖新版本、超时写入明确返回“结果不确定且本地已应用”。

这仍然不是 Raft/Paxos，也不提供持久化、自动选主、写前日志、反熵修复或跨 Leader 冲突解决。项目定位是可解释的一致性/延迟权衡实验，而不是生产级数据库。

## 原实现问题与改进

| 原问题 | 本次实现 | 可验证结果 |
|---|---|---|
| `get` 后 `put` 分配版本，并发写可能得到重复版本 | 对每个 key 使用 `ConcurrentHashMap.compute` 原子递增并安装新值 | 同 key 并发测试通过 |
| 写复制串行，达到 W 后不再发送剩余副本 | 有界线程池并行 fan-out；达到 W 即响应，已提交任务继续 | 慢副本不阻塞快速 quorum，且 W=1 仍会向所有副本发送 |
| 复制乱序会让旧值覆盖新值 | follower 仅接受更高版本；同版本不同值返回 409 | 旧版本覆盖与同版本冲突测试通过 |
| 读请求把 404 当异常，无法证明“多数副本都没有该 key” | 可达副本的 404 计入读 quorum；从已收集响应选择最高版本 | 缺失 key 返回 404，新旧副本并存时返回最高版本 |
| 请求可能无限受慢节点影响，失败含义不清 | 连接/读取/quorum 超时可配置；未达到 W 返回 503、`status=indeterminate`、`localApplied=true` | 超时部分写入测试通过 |
| 配置缺少范围检查 | 启动时校验 W/R、超时、延迟、线程数；URL 去空、去重 | 非法配置提前失败 |
| 缺少运行可观测性 | `/health` 暴露角色、失败复制数、队列和活跃任务数 | 可用于演示与故障定位 |

## 语义说明

- 写入只接受 Leader 的 `PUT /kv`。Leader 先原子更新本地值，再向所有配置副本提交复制任务。
- 收到包含 Leader 自身在内的 W 个确认后返回 201。任务已经提交但未完成的副本会继续执行；当前没有持久化重试队列。
- 到达 `QUORUM_TIMEOUT_MS` 仍未达到 W 时返回 503。因为本地可能已更新、部分副本也可能稍后成功，客户端不能把它解释为“肯定没有写入”；响应显式标记 `indeterminate`。
- `GET /kv/{key}` 收集 R 个可达节点的回答并返回最高版本。达到 R 且全部为缺失时返回 404；不能达到 R 时返回 503。
- W+R>N 只在单 Leader、单调版本、节点集合稳定等假设下帮助获得最新值；本实现没有选主协议，不能在网络分区中保证生产级线性一致性。

## 自动测试

2026-09-13 最终执行 Maven 测试：10 项全部通过，0 failure、0 error。

| 测试组 | 数量 | 覆盖重点 |
|---|---:|---|
| `KvCorrectnessTests` | 3 | 乱序复制、读 quorum、同 key 并发版本 |
| `KvReplicationTests` | 6 | 慢副本、W=1 全量 fan-out、超时部分写、缺失 key、最高版本读取、冲突与非法输入 |
| `NodeApplicationTests` | 1 | Spring 上下文加载 |

旧实现以相同正确性测试执行时为 3/3 失败；这组对照证明修复针对的是可复现缺陷，而不是只增加测试数量。

## 基准方法

`benchmark.py` 使用 Python 标准库启动 5 个真实 JVM 节点，以固定 seed、20 个热 key、16 个并发线程、200 次预热和 600 次计量请求运行。标准矩阵每个 case 重复 3 次，报告中位数；保存逐请求记录、每次运行 CSV、聚合 JSON、环境信息和 JAR/脚本 SHA-256。

| 场景（中位数） | TPS | 成功率 | Mean | P95 | P99 | stale rate |
|---|---:|---:|---:|---:|---:|---:|
| W3/R3，50/50，无延迟 | 1,145.4 | 100% | 13.2ms | 36.3ms | 47.8ms | 0% |
| W1/R1，50/50，无延迟 | 1,549.0 | 100% | 10.0ms | 24.8ms | 38.0ms | 0.67%（中位） |
| W5/R1，50/50，无延迟 | 1,076.3 | 100% | 14.6ms | 31.6ms | 38.2ms | 0% |
| W1/R5，50/50，无延迟 | 990.2 | 100% | 15.7ms | 32.4ms | 46.0ms | 0% |
| W3/R3，复制延迟 50ms | 355.6 | 99.83% | 43.9ms | 87.2ms | 95.4ms | 0% |
| W3/R3，复制延迟 200ms | 144.4 | 100% | 108.0ms | 230.4ms | 237.5ms | 0% |
| W3/R3，一个慢副本 | 123.3 | 100% | 124.0ms | 375.9ms | 469.8ms | 0% |
| W3/R3，一个不可达副本 | 1,249.7 | 100% | 12.2ms | 25.4ms | 34.1ms | 0% |

W1/R1 的局部读允许看到复制滞后，因此出现非零 stale rate；提高读取或写入 quorum 后，本次矩阵记录未观察到 stale read。这里的 0% 是有限样本观测，不等于理论上永不陈旧。

高延迟同口径（复制 200ms、Leader 写延迟 200ms、读延迟 50ms）中，旧 JAR 中位 TPS 为 44.1，新 JAR 为 64.5，提升约 46.2%；Mean 从 353.7ms 降至 244.1ms，P95 从 649.7ms 降至 433.3ms。该对照受本机 JVM 调度影响，适合项目演示，不应当作跨机器容量承诺。

矩阵中少数高并发重复运行出现瞬时请求失败，聚合文件完整保留成功率，未通过删掉异常轮次美化结果。这提示下一阶段应加入受控到达率、客户端重试分类和服务端线程池指标。

## 运行与证据

```powershell
cd node
mvn test
mvn package
cd ..
python benchmark.py --requests 600 --warmup 200 --output evidence/benchmark
```

- 聚合矩阵：`evidence/benchmark-final/summary.json`
- 每轮结果：`evidence/benchmark-final/results.csv`
- 逐请求记录与节点日志：`evidence/benchmark-final/case*/`
- 旧实现对照：`evidence/baseline-benchmark/`
- 自动测试：`node/src/test/java/com/kv/node/`

## 后续边界

若要继续向生产系统靠近，优先级应是：持久化日志与崩溃恢复、请求幂等键、后台重试/反熵、成员变更与健康摘除、Leader epoch/共识、认证与限流。当前停止在提升计划要求的原子版本、并行 quorum、超时语义、故障测试和基准矩阵范围内。
